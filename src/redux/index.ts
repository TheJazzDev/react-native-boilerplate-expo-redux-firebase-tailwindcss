export * from './features';
import { rrfProps } from './reactReduxFirebaseConfig';

// import { store, dispatch } from './store';
import { store, dispatch } from './store2';

export { store, dispatch, rrfProps };
